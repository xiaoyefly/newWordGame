import { Blockchain, Config, Endpoint } from './types';
declare class CyberConnect {
    address: string;
    namespace: string;
    endpoint: Endpoint;
    resolverRegistry: any;
    signature: string;
    chain: Blockchain;
    chainRef: string;
    provider: any;
    signingMessageEntity: string | undefined;
    constructor(config: Config);
    getAddress(): Promise<any>;
    authWithSigningKey(): Promise<void>;
    follow(address: string, handle: string): Promise<void>;
    unfollow(address: string, handle: string): Promise<void>;
}
export default CyberConnect;
//# sourceMappingURL=cyberConnect.d.ts.map